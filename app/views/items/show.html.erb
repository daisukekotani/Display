<h1>item show</h1>

<p><%= attachment_image_tag @item, :image, :fill, 190,  250, format: 'jpeg' %></p>
<p><%= @item.name %></p>
<p><%= @item.url %><p>

<%= link_to '戻る', items_path %>
<%= link_to '編集', edit_item_path %>
<%= link_to '削除', item_path(@item.id), method: :delete %>