	<%= form_for(@item) do |f| %>
    <div class="field">
      <%= f.label :画像 %>
      <%= f.attachment_field :image %>
    </div>
    <div class="field">
      <%= f.label :名前 %>
      <%= f.text_field :name, class: "name" %>
    </div>
	<%= f.submit %>
	<% end %>